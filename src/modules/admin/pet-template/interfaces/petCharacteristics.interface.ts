import { PetTemplateTraits } from "./petTemplateTrais.interface";

export interface PetCharacteristics {
  primaryColor: string;
  secondaryColor: string;
  coatPattern: string;
  coatLength: string;
  traits: PetTemplateTraits;
}